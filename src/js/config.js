const config = {

    locationAccuracy: {
        defaultVal: -55,
        lowVal: -60,
        highVal: -30,
    },

    surveillanceMap: {
        intevalTime: 1000,
        mapBound:[[0,0], [21130,35710]],
    },
    
    locale: {
        defaultLocale: 'en'
    }
}

export default config;